import {expect, test } from "vitest"
// import { Parse } from "./grammar";

test("testing grammar", () => {
    const testRnd = "Rnd 1: sc";
    debugger;
    // Parse(testRnd);
    expect([1]).length(1);
});